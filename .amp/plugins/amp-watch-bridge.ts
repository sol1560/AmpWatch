// Project plugin entry. The real code lives in Plugin/ so it can be tested and
// reviewed like any other source; this file only exists because Amp discovers
// project plugins under .amp/plugins/.
export { default } from '../../Plugin/amp-watch-bridge'
